# Spark, and the reference vocabularies it reads.
#
# The first half of this stage - dismantling the raw crawl into the shape
# Spark can read - is standard library only, and deliberately so: it is a
# change of shape, not of meaning. Nothing below is needed for it.
#
# PySpark needs a JVM on PATH - Spark 4 wants Java 17 or 21:
#
#     JAVA_HOME=$(/usr/libexec/java_home -v 21) python cut.py
pyspark>=4.0.0          # pulls py4j, and provides spark-submit itself
PyYAML>=6.0.3           # reference/*.yaml

# Not imported by this code, but needed to open the output with pandas
# (pd.read_parquet) or to use toPandas(). Without it pandas raises
# "Unable to find a usable engine".
pyarrow>=17.0.0

# `python cut.py upload` puts the parquets into Snowflake's PROCESSED stage.
# The tables themselves are loaded from there by sew.
snowflake-connector-python>=3.12.0   # see scripts/upload_processed.py
python-dotenv>=1.0.0                 # AMARA_SNOWFLAKE_* from .env
