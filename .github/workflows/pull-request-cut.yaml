name: pull request cut

on:
  pull_request:
    paths:
      - 'cut/**'
      - '.github/workflows/pull-request-cut.yaml'
      - '.github/actions/build-image/action.yml'

permissions:
  contents: read

# A new push supersedes the run in flight for the same pull request.
concurrency:
  group: pr-cut-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  build:
    name: build amara-cut
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/build-image
        with:
          component: cut
